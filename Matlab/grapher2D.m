

%plotPositions(P , count, simulationStep);
%plotBoundaries(P , B,  count, simulationStep);
plotLeaders2D(P ,  count, leader_count, simulationStep);
plotConnections2D(P , B, G, count, leader_count, simulationStep);
